# # Web Server
# server.port=8082

# # MySQL Connection (Now with auto-create enabled)
# spring.datasource.url=jdbc:mysql://localhost:3306/Banking?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=dogiathuy1

# # Hibernate Configuration
# # Giữ nguyên tên bảng và cột chính xác như trong @Table và @Column

# spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
# spring.jpa.hibernate.ddl-auto=none
# spring.jpa.show-sql=true



# Web Server Configuration
server.port=8082

# MySQL Connection (Secured with Placeholders)
#
spring.datasource.url=jdbc:mysql://localhost:3306/Banking?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=dogiathuy1

# Hibernate / JPA Configuration
# Sử dụng PhysicalNamingStrategyStandardImpl để giữ nguyên tên cột/bảng như trong code
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

# JWT Security Configuration (Added placeholder for safety)
#
banking.app.jwtSecret=${404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
banking.app.jwtExpirationMs=86400000